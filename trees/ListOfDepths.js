class TreeNode {
    constructor(value) {
        this.value = value
        this.left = null
        this.right = null
    }
}

// Given a binary tree, design an algorithm which creates a linked list of all the nodes at each depth
// en. if you have a tree with depth D, you'll have D linked list

